ARG_factry = {
	enable = {
		AND = {
			tag = ARG
			has_war = no
		}
	}
	
	abort = {
		AND = {
			tag = ARG
			has_war = yes
		}
	}

    ai_strategy = {
        type = added_military_to_civilian_factory_ratio
        value = 25
    }
}
ARG_factry_senzi = {
	enable = {
		AND = {
			tag = ARG
			has_war = yes
		}
	}
	
	abort = {
		AND = {
			tag = ARG
			has_war = no
		}
	}

    ai_strategy = {
        type = added_military_to_civilian_factory_ratio
        value = 40
    }
}
